<Page
    x:Class="Fzuhelper.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fzuhelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="AliceBlue">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <RelativePanel Width="300" Height="200">
            <Image x:Name="loginLogo" RelativePanel.AlignHorizontalCenterWithPanel="True"/>
            <TextBox x:Name="username" PlaceholderText="账号" RelativePanel.Below="loginLogo" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="0 10 0 0" Width="200" InputScope="Number"/>
            <PasswordBox x:Name="password" PlaceholderText="密码" RelativePanel.Below="username" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="0 10 0 0" Width="200"/>
            <Button x:Name="loginBtn" Content="登录" Click="loginBtn_Click" RelativePanel.Below="password" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="0 10 0 0"/>
        </RelativePanel>
        <ProgressRing x:Name="logingIn" Height="150" Width="150" IsActive="False" />
    </Grid>
</Page>
